let numeros = [1, 2, 3, 4, 5];

function mostrarNumeros(numeros) {
    let primeiro = numeros[0];
    let segundo = numeros[4];
    console.log(primeiro + ' and ' + segundo);
}

mostrarNumeros(numeros);