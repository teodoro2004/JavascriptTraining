let array = [100, 200, 300, 400, 500, 600];

function somarTudo() {
      let soma = 0;
      for (let i = 0; i < array.length; i++) {
        soma += array[i]
      } 
      console.log(soma);
    }
  somarTudo();