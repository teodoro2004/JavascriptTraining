let maiorNumero = 40;

function contarNumerosPares(maiorNumero) {
    for (let imprimir = 2; imprimir <= maiorNumero; imprimir += 2){
        console.log(imprimir);
    }
    }
    contarNumerosPares(maiorNumero);

